
const numbers = [1, 2, 3, 4, 5]

const dobro = (n) => n * 2

// console.log(numbers.map(dobro))

// console.log(numbers.map(function(item){
//   return item * 2
// }))

// console.log(numbers.map((item, pos, arr) => {
//   return item * 2 + pos + arr
// }))

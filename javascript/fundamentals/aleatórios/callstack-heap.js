var num = 1 // Aloca memória pro tipo int||float
var str1 = "Hello World" // Aloca memória pro tipo string

// Aloca memória para esse Objeto
var name = {
  first: 'Daniel',
  last: 'Marques'
}

// Aloca memória para a função
function addOne(a){
  return display(a+1)
}

// Aloca memória para essa função
function display(result){
  console.log(result)
}

addOne(1)
